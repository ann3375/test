export enum SCREENS {
  SCREEN_HOME = '/',
  SCREEN_LOGIN = '/login',
  SCREEN_SIGN_UP = '/signup',
  SCREEN_CURRENT_DIALOG = '/dialogs/:id',
  SCREEN_DIALOGS = '/dialogs',
}

export enum Pages {
  auth = 'auth',
  chat = 'chat',
}
